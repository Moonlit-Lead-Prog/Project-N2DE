version: D0.1.{build}

configuration: Release

image: Visual Studio 2017

install:
  appveyor DownloadFile https://www.libsdl.org/release/SDL2-devel-2.0.8-VC.zip 
  appveyor DownloadFile https://www.libsdl.org/projects/SDL_image/release/SDL2_image-devel-2.0.3-VC.zip 
  
  7z x SDL2-devel-2.0.8-VC.zip > nul 
  
build:
  verbosity: normal
